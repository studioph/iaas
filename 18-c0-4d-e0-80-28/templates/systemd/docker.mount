{{- $fs := .Values.storage.filesystems.docker -}}
[Unit]
Before=local-fs.target

[Mount]
What={{ $fs.device }}
Where=/var/lib/docker
Type={{ index $fs "format" | default .Values.global.defaults.storage.format }}

[Install]
WantedBy=local-fs.target